import { Route } from '@angular/router';
import { SimulationComponent } from './index';

export const SimulationRoutes: Route[] = [
    {
        path: 'simulation',
        component: SimulationComponent
    }
];